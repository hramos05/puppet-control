---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: "Yaml backend"
    data_hash: yaml_data
    paths:
      - "nodes/%{::trusted.certname}.yaml"
      - "roles/%{::trusted.extensions.pp_role}.yaml"
      - "hardening/%{::trusted.os.family}-%{::trusted.os.release.major}.yaml"
      - "os/%{::trusted.os.name}.yaml"
      - "domains/%{::trusted.domain}.yaml"
      - "common.yaml"
